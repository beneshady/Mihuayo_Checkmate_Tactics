# US-006：敌方先准备、方向锁定与解谜回合

状态：Product acceptance failed / 待开发修复。优先级：P0。
工作目录：`D:\Projects\mihuayou\gamehack2026`；分支：`codex/web-gameplay-validation`。
代码基线：`7e3c5ab`（功能 `448ccdc`）；流程：策划 → 开发 → 策划验收。
父需求：[M0](../design/m0-gdd.md)；设计背景：[V1 整合稿](../design/V1-Development-Handoff.md)。

## 1. 用户确认与范围

2026-09-24 用户确认：敌方先行展示攻击意图，我方行动后敌方才攻击；意图确定后不追踪移动目标、不重新瞄准。敌人被推动时“保持攻击方向、落点随位置变化”，并明确要求交接开发整改。

目标是可预见、可干预的解谜战棋。设计参考 [ITB 设计复盘](https://www.gameres.com/905495.html)，但以下是本项目的规则合同，不声称完整复刻参考作品。

本 US 覆盖旧文档中“锁定绝对目标格”“被推后朝原绝对格重新检查”“敌方移动与攻击二选一后才交给玩家”的冲突描述。本次胜负按实际主帅阵亡结算，不再因搜索得出将死而提前终止玩家操作。

工作区另有未提交的 V1 成长改稿（取消技能点、升级选择技能），不是本任务的实施范围；保留文件，不据此顺带实现或提交。只把本 US 的回合规则作为本次增量合同。

## 2. 回合合同

### 2.1 敌方准备 → 玩家行动 → 敌方攻击

1. 开局、下一波入场以及每次敌方攻击结算完毕后，进入敌方准备。
2. 敌方按稳定顺序各获得至多一次普通移动（可不移动），只能移动到合法空格，不在准备阶段攻击、冲击、造成伤害或触发击杀成长。移动距离、棋种走法、九宫和阻挡沿用当前规则。
3. 所有敌方准备移动完成后，根据这一最终局面，为每个敌人生成至多一次攻击意图；无合法攻击则明确显示待机。本批意图统一冻结，然后恢复我方回合额度并交给玩家。
4. 玩家移动、攻击、升级、查看预览和撤销均不重新生成敌方意图。仅当前预计落点、伤害与有效性随局面刷新。
5. 玩家确认结束回合后，敌方按预告顺序各执行至多一次冻结攻击；这一阶段不新增普通移动、不补选攻击。
6. 结算结束且战斗继续，才进入下一次敌方准备。开场与新波次必须先给玩家处理意图的机会，不允许先扣血。

敌方 AI 采用最小确定性实现：准备时已有合法攻击就保持原位；否则按既有“接近我方帅、同距按坐标”的规则选择普通移动。逐个移动时使用实时占位，禁止叠子或穿子。全部移动完成后选攻击，优先帅、车、其他目标，同优先级沿用坐标排序。敌方次序采用稳定的出生/配置顺序，显示的攻击顺序必须等于实际执行顺序，不引入随机性或复杂搜索。

### 2.2 锁定相对攻击，而不是受害者

- 冻结攻击方式、方向、相对攻击距离/形状与顺序，不存活目标追踪，不锁死世界坐标。
- 最小实现口径：保存预告时“攻击格减攻击者位置”的相对位移。结算目标格 = 攻击者当前实际位置 + 原相对位移。直线攻击保持原方向和距离；不因被推而延长射程、缩短到另一个目标或旋转方向。
- 例如敌方车从 `(4,4)` 向 `(4,3)` 冲击，被横推到 `(3,4)` 后，仍向下冲击 `(3,3)`，绝不回头攻击原 `(4,3)`，也不追踪原目标。
- 对兵、士、将（及规则支持的马），保留原一步/日字相对形状。当前九宫、马腿等约束仍适用；不能合法执行就明确标记失效，不另选动作。
- 连续推动累计改变攻击者位置；撤销我方移动恢复对应局面与预测，但原冻结决策保持一致。

### 2.3 结算与干预

| 局面变化 | 结算规则 |
| --- | --- |
| 原受害者走开 | 攻击原相对落点，允许落空，不追击新位置 |
| 攻击者阵亡 | 跳过其攻击；后续顺序不重排 |
| 新落点被另一棋子占据 | 命中当前占位者，包括敌方自己的同伴；不能因为是同阵营而取消 |
| 敌人误杀敌人 | 不给我方任何棋子击杀升级、技能点或击杀统计奖励；敌方自身也不成长 |
| 炮的炮架变化 | 从当前炮位置到平移后落点之间仍须恰好一个炮架；不足或多于一个则失效，不搜寻替代目标 |
| 炮落点为空 | 炮架条件成立则射空；攻击力仍为2，敌方无扩散技能，只命中该落点一枚单位 |
| 冲击落点为空 | 路径合法则按既有车规则冲到落点；不产生伤害或成长 |
| 路径、宫界、马腿不合法或落点越界 | 本次攻击失效，显示原因；不新增绕行、穿透、碰撞伤害、替代目标 |
| 前序攻击改变后序的炮架、占位或存活 | 后序攻击使用执行时局面，但保持原意图与顺序；预览必须按同样的顺序预测 |

仅改变敌方冻结攻击的友伤规则；不把我方普通攻击改为可随意点杀友军，不改变我方炮现有扩散友伤、技能数值或推动限制。不新增尸体或地形系统。

### 2.4 胜负与波次

- 我方帅实际阵亡立即失败；不因危险提示或“无解”搜索提前判负、清除意图或禁用撤销。
- 敌将实际阵亡结束当前波；第一波保留原商店，第二波胜利。不把两波流程擅自改成单波。
- 主帅阵亡后停止后续敌方攻击和准备，不再对已结束波次移动、扣血或发奖励。
- 搜索可保留为提示辅助，但不能改变战斗状态。已有将死夹具、断言与 UI 文案须按此更新，不通过删除无关回归掩盖失败。

## 3. 展示与交互

- 非悬停时默认展示所有敌方攻击落点/威胁格及行动顺序，使用既有整格覆盖风格。重叠威胁不能无声覆盖丢失，可结合已有意图列表显示多个攻击者。
- 悬停敌人突出该敌人的方向、路径、预计伤害、顺序及当前结果（命中/落空/失效/待机），离开恢复总览与原选择。不再需要逐个悬停才能看到整体威胁。
- 推动、击杀、移动、撤销后立即更新预测；敌方尚未执行时不能伪造新的 AI 决策。失效攻击保留可读说明。
- 玩家攻击预览和敌方威胁采用可区分样式，不回退到格内圆圈。预览、详情与真正结束回合使用同一套结算语义。
- 保留 US-004 直接移动/撤销、US-005 我方炮移动后攻击与双额度，不增加攻击后移动。

## 4. 验收条件

| 编号 | 可观察结果与最低验证 |
| --- | --- |
| AC01 | 默认新局与第二波均先完成合法敌方准备，再展示意图并交给玩家；玩家第一次结束回合前无敌方伤害，准备无叠子、非法移动或重复执行。 |
| AC02 | 一个敌人可在准备阶段移动后预告攻击；攻击阶段只执行冻结攻击、不再普通移动；新回合只重新准备/生成一次意图。 |
| AC03 | 原目标移走时不追踪；受推敌人沿原方向、原距离在新位置攻击，横推与纵推、连续推动至少各有规则用例；不回打原绝对格。 |
| AC04 | 新落点可伤敌方同伴且不会给我方成长或击杀数；敌人提前阵亡跳过；落点空格时原地攻击落空、车合法冲空，均不伪发奖励。 |
| AC05 | 炮新路径恰好一个炮架才射击且单目标伤害2；覆盖炮架移走、增加第二架、推动炮后炮架成立/失效。宫界、越界、马腿、冲击阻挡不合法时有明确失效原因且不重选目标。 |
| AC06 | 默认整格展示全部威胁和顺序；悬停突出单敌；推后、撤销后、击杀后刷新正确，无旧格残留。含前序攻击改变炮架/杀死后序敌人的用例，预览与实际结算一致。 |
| AC07 | 帅活着时不会因将死搜索强制失败；帅实际阵亡立即失败；第一波杀将进商店、第二波杀将胜利；终局后不再执行后续敌方动作。 |
| AC08 | 我方车/炮双额度、直接移动、移动撤销与攻击锁定、暂停恢复、9×9点选、现有技能均回归通过；正式两波马/炮回放根据新流程重新生成并验证，不能把旧回放失效误报为新流程已通过。 |
| AC09 | 不触碰既有对局标签，不刷新/导航/关闭/注入、不清存储、不改全局视口、不停启已有服务；策划仅用新建独立标签试玩，不承诺正在进行的旧局热更新。 |

开发提供新的可复现意图试玩夹具：逃离目标、横推改线、误伤同伴、炮架变化、顺序连锁；夹具通过自然点击即可验证，不要求策划注入状态。默认新局及完整两波也必须验证，不能仅靠夹具。

## 5. 验证基线、遗留事项与交付要求

- 交接前只完成需求与当前实现检查，尚未实现本 US。旧功能基线此前通过规则21/21、回放2/2、UI6/6及静态/语法检查；这些不是本 US 通过证据。
- US-005 未验收完成：新标签已验证炮移动后攻击、撤销恢复、攻击后锁定、下回合恢复；发现撤销攻击预览后“执行攻击”按钮仍可点击但没有效果。详见 US-005 的 PA-C5-01；该遗留单独保留，不宣称已修复或已验收，本 US 不顺带接管其任务。
- 保护现有未提交 `docs/design/m0-gdd.md`、`docs/design/unit-skills-v1.md`、`docs/design/V1-Development-Handoff.md` 与 `outputs/01a0bf14-7f85-7282-bb8d-672064556a58/美术资产清单.xlsx`。禁止整目录暂存、切换共享分支、推送、合并或部署。
- 开发只实施本 US。先记录 HANDOFF_ACK；遇无法按合同执行的关键歧义回策划，不自行引入新规则。实现后记录变更文件、功能提交、测试命令/结果、试玩步骤、遗留风险，并冻结版本直接发送策划 `01a06c5f-88f7-7b40-bc29-6e2885783326` 验收；不转独立测试会话。
- 本 US 验收通过也不代表 US-005、US-002 或整体 M0 完成。

## 6. 实施与策划验收记录

- 开发：已实现敌方“准备移动 → 冻结意图 → 玩家行动 → 顺序攻击”循环。开局、跨波和每次攻击结算后均先逐个准备；有攻击则原地、无攻击才按距帅和坐标靠近。冻结意图保存相对位移，推移攻击者后落点跟随平移；炮架、宫界、马腿、冲击路径均在执行时重验，不替换决策。敌方可命中当前落点的同伴，且不会产生我方成长；主帅仅实际阵亡失败。UI 默认展示全部威胁格和顺序，悬停显示冻结起点、当前路径、落点和失效原因。新增 P9 `escape`、`push-line`、`friendly-fire`、`cannon-rack`、`chain` 夹具。
- 验证：`node --check web/js/rules.js`、`node --check web/js/app.js`；规则 22/22；UI 桩 7/7；重新生成正式马/炮回放 2/2（马 9 步/3 回合、炮 8 步/3 回合）；静态检查 40 DOM / 20 API 与 `git diff --check` 均通过。开发未操作浏览器、服务、标签、视口或存储。
- 策划验收（`2b4f620`，2026-09-24）：独立规则22/22、回放2/2、UI7/7、语法、静态及差异检查通过。浏览器仅使用本轮新建标签和新启动的8771静态服务（启动前确认端口无监听），未操作任何既有对局或服务。
- 已试玩：默认开局敌人已准备、帅HP3/3和炮HP2/2无开场伤害；P9 push-line自然冲击后敌车 `(3,3)→(4,3)`、意图落点 `(3,2)→(4,2)`，结束回合后进入下一轮准备；friendly-fire结束回合后同伴阵亡，我方击杀0、帅Lv1/SP0不变；cannon-rack马 `(2,3)→(4,2)` 后显示“缺少炮架”，撤销后恢复“可执行”；chain预告两次可执行，结束后马和车均阵亡、帅存活，符合先杀马再炮击的顺序预测。上述页面浏览器日志无warn/error。
- 本轮不通过：PA-I6-01、PA-I6-02 见下。默认完整两波、实际帅阵亡、逃离和部分边界仍须在修复冻结版补验；已通过自动回放不能冒充真人完整两波试玩通过。US005及父M0保持未完成。

### 策划退回缺陷与补证

| 编号 | 范围/严重度 | 最小复现与实际结果 | 修复/补证要求 |
| --- | --- | --- | --- |
| PA-I6-01 | AC06 / P1：阵亡威胁残留 | 默认新局选初始炮 `(1,2)`，预览并执行击杀敌炮 `(1,7)`，点击取消清除选择。列表显示 `5 w1-4 · 攻击 / 已移除`，但棋盘 `(1,2)` 仍有与有效威胁相同的紫色整格。已用真实截图确认，不是选中格或攻击预览。 | 阵亡/预测在前序中阵亡的攻击者不得继续显示有效威胁。可保留“已移除”历史说明但不能误导仍会攻击；未知内部ID不应替代原棋种名称。至少补UI回归：击杀敌炮后无旧威胁覆盖，其他存活敌人威胁保留。关注 `renderIntentOverview` 对removed状态的处理。 |
| PA-I6-02 | 第3节、AC06 / P2：悬停缺关键信息 | 默认局悬停敌炮：仅显示敌方炮、冻结/当前位置、落点、“可执行”和路径，没有预计伤害、受击对象/HP变化或行动顺序。P9 push-line同样没有顺序和伤害信息。 | 悬停明确显示预告顺序与预计命中/伤害；使用按固定顺序预测所得结果，准确区分落空、友伤、阵亡和失效，不能只是静态攻击力；补UI断言。 |

- 补充验收证据：新测试只覆盖横推一次；请依AC03补纵推、连续推动保持原offset。依AC05补敌炮被推后炮架成立/失效、两炮架、越界/宫界/马腿/冲击阻挡。依AC06/07补前序击杀后序攻击者与主帅死亡立即停止后续动作。这些是原合同要求，不是新增玩法；用聚焦规则用例补齐，不删除无关回归。
- 修复只限上述US006缺口，US005 PA-C5-01继续独立保留。修复后再次冻结交策划，届时继续未完成的正式两波及边界试玩。

## Agent Handoff Audit

| Audit ID | Time (UTC+8) | Event | From role / thread ID | To role / thread ID | Message ID or title | Summary | Evidence links | Result |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| H-US-006-001 | 2026-09-24T16:27:55+08:00 | US_READY | 策划 / 01a06c5f-88f7-7b40-bc29-6e2885783326 | 开发 / 01a07037-771d-7322-9ffc-00310bb38c60 | 开发交接｜US-006｜敌方先准备与方向锁定整改 | 用户确认方向锁定、受推落点平移并授权实施；交接回合、友伤、预览、实际阵亡结算和AC01–09；保护用户旧局与其他未提交设计，US005未完成独立保留 | 3e97dda；代码基线7e3c5ab；本文；直接交接消息（工具未返回message ID） | 发送成功，待开发确认接手；不代表实现完成 |
| H-US-006-002 | 2026-09-24T16:28:19+08:00 | HANDOFF_ACK | 开发 / 01a07037-771d-7322-9ffc-00310bb38c60 | 策划 / 01a06c5f-88f7-7b40-bc29-6e2885783326 | US-006 接手确认 | 已核对合同、基线、范围和受保护的无关改动；开始规则与 UI 实现 | 本文；3e97dda；开发会话消息 | 已接手 |
| H-US-006-003 | 2026-09-24T20:35:52+08:00 | DEV_COMPLETE_HANDOFF | 开发 / 01a07037-771d-7322-9ffc-00310bb38c60 | 策划 / 01a06c5f-88f7-7b40-bc29-6e2885783326 | US-006 冻结交付 | 已实现敌方准备、相对方向锁定、友伤和整格预告；新增 P9 意图夹具 | 47984d2；规则22/22；UI7/7；回放2/2；静态检查通过 | 待策划验收 |
| H-US-006-004 | 2026-09-24T20:37:10+08:00 | HANDOFF_ACK | 策划 / 01a06c5f-88f7-7b40-bc29-6e2885783326 | 开发 / 01a07037-771d-7322-9ffc-00310bb38c60 | 接手US-006独立验收 | 已核对47984d2/2b4f620与保护文件；独立规则22/22、回放2/2、UI7/7、语法及静态通过，开始新标签试玩 | 2b4f620；本文；开发直接消息 | 验收中，版本冻结 |
