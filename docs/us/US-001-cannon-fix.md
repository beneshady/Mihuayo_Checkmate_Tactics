# US-001 补充修复：炮基础攻击与独立验证

状态：Completed（仅本炮补充修复）；实现提交：`7392116`；验收冻结版：`7e82212`；父任务：[US-001](US-001-m0-player-skills.md) 仍待整体最终验收。
基线：`af25dfa`；分支：`codex/web-gameplay-validation`。
工作目录：`D:\Projects\mihuayou\gamehack2026`。
本次流程按最新 AGENTS：策划 → 开发 → 策划验收，不设独立测试交接。

## 用户确认与范围

用户确认“初始攻击 1 格”指只命中一个目标格，不是射程一格。炮无需升级即可隔恰好一个炮架，对横竖同线敌人造成 2 伤害；初始不带溅射。升级仅扩大目标周围的固定溅射格。

用户授权修改，但必须保留其正在进行的游戏；提供独立炮验证入口供直接体验。现有移动/攻击共用 1 AP 的规则不变，不增加先移后攻，不修改部署、伤害、升级价格或其他玩法。

## 已复现问题与边界

策划已运行纯规则反例：零 SP、cannonMask=0 的新炮可隔友军马击杀 HP2 敌车，获得等级及 1 SP。炮先移动后 AP=0，legalActions 仍列出同线隔一子攻击，而 preview=null、submit 拒绝；页面 renderMarks 用 legalActions 高亮，target 只提示“该目标不合法”。这是已复现的提示与执行不一致，不等于已定位用户当时局面的全部原因。

上一轮本地 8765 页面服务连接被拒绝，未声称已复现该用户局面的浏览器操作。

## 验收条件

1. 基础攻击零 SP、零升级可用：同线恰好一个炮架，主目标伤害 2、只命中目标格；友军和敌军均可作炮架；零或两个炮架、斜线拒绝，非法操作不耗 AP。
2. 展示“基础攻击：单格 / 伤害 2 / 隔一子攻击”、当前行动可用/已用；扩散 0/8 不暗示无基础攻击。选中合法目标时能辨认炮架、攻击连线、目标和预期伤害。
3. 行动耗尽后不得继续给炮可执行攻击高亮或启用执行；明确提示“本回合已行动，下回合可攻击”。无炮架、多个炮架、不在同一直线等失败原因分开反馈。UI 与真实提交一致，不破坏敌方意图生成与将死搜索。
4. 新增现有 P3/P4 等夹具的明确变体或同等最小独立入口：初始炮 Lv1、SP0、无扩散、AP1；提供就位炮架和可立即攻击的敌人。使用真实规则跑完“基础击杀 → 获得 1 SP → 花点购买扩散 → 下回合再次攻击”，不注入赠送点数、不直接改状态。保留敌将或其他目标避免首次击杀结束体验。页面清楚标识测试夹具，正式局不受影响。
5. 原有 P1–P8 与正式马/炮回放回归通过；开发新增针对基础射击、移动后/AP耗尽、下一回合恢复、零SP成长升级的规则/UI检查，记录实际浏览器结果（未做则明确标记）。只用现有网页工具链，不引入服务器热重载。
6. 当前游戏保护为硬约束：不选择、刷新、导航、关闭、重开、点击或向用户现有任何游戏标签注入代码；不清理缓存、storage、cookie、不改全局浏览器视口；不停止/重启用户服务，不杀占用端口进程。只在新建独立标签验收。旧标签保持已加载旧代码，不能宣称旧局已热更新。若需本地服务，仅在确认未监听后另起静态服务或选空闲端口，绑定 127.0.0.1，无自动刷新；后台工具新开标签可能触发现有页面自身的自动暂停，但不得更改棋局。

## 交付与验收

开发提交范围仅 web、对应测试和本补充记录/必要启动说明；保留 AGENTS.md、角色登记、主 US 未提交记录和 m0-zyf/。完成后直接交策划任务 `01a06c5f-88f7-7b40-bc29-6e2885783326`，提供冻结提交、独立验证 URL、步骤、命令与结果、未验证风险。不唤醒历史测试任务，不推送/合并/部署。

策划只接受本补充修复，不代表父 US 或完整游戏已最终验收。

## 实现和验证记录

- 接手基线：`af25dfa`；实现提交：`7392116`。实现范围只含 `web/`、对应测试和本补充交付文档，未纳入 `AGENTS.md`、角色登记、父 US 脏改或 `m0-zyf/`。
- 规则：玩家非车单位 AP 为 0 时 `legalActions` 返回空，和 `preview` / `submit` 一致；新增纯规则失败原因，区分本回合已行动、无炮架、多个炮架、非横竖同线、移动阻挡和移动超距。
- 展示：炮选择卡显示基础单格攻击、伤害 2、隔一子、行动状态和扩散不影响基础攻击；合法攻击预览显示炮架、完整连线、主目标与预期伤害；无效选择禁用执行并显示具体原因。
- 独立入口：`http://127.0.0.1:8766/?fixture=P3&variant=basic-growth`。初始炮 Lv.1 / SP0 / 扩散0/8 / AP1；真实击杀 `(2,4)` 后得 1 SP，购买 `扩散 E`，结束回合，再攻击 `(5,1)` 并覆盖 `(6,1)`；保留敌将。
- 自动验证：`node --check web/js/rules.js`、`node --check web/js/app.js`、规则 18/18、正式回放 2/2、静态检查 38 DOM / 19 API、UI 桩 2/2、`git diff --check` 均通过。开发按硬约束未打开、选择、刷新、关闭、导航或注入任何浏览器标签，也未启动 / 停止服务或改 viewport；真实浏览器尚未由开发执行，交策划在其独立标签验收。

## 策划浏览器验收（2026-09-20）

结论：**PRODUCT_ACCEPTED，仅本炮补充修复**。当前炮可直接进行基础攻击与真实击杀成长；父 US-001 / M0 整体未完成最终验收。下一项为父 US 既有交付的剩余策划验收，处于待继续验收状态，不新增开发范围。

- 环境：`codex/web-gameplay-validation`，冻结版 `7e82212`，核对 `web/`、`tests/` 无工作区差异；新建独立浏览器标签 3，页面清楚标识 `测试夹具 P3 · basic-growth`。未改变浏览器视口。
- 基础到成长：选炮 `(2,1)`，隔友军马 `(2,2)` 攻击兵 `(2,4)`，预览显示炮架、完整连线及主目标预期 −2；执行后 Lv2 / SP1 / AP0 / 击杀1。再点敌车显示“本回合已行动，下回合可攻击”，执行禁用且可执行落点清空。
- 真实购买与下回合：花上述 1 SP 买 `扩散 E`，SP0 / 扩散1/8；确认结束回合后 AP1。重新选炮打 `(5,1)`，隔友军马 `(3,1)`，预览及结算同时杀 HP2 车与邻兵 `(6,1)`；最终 Lv4 / SP2 / 击杀3 / AP0，敌将 HP2/2，体验未提前结束。
- 初始单目标：仅重置独立夹具，零升级炮直接打 `(5,1)`，实际伤害2杀死车；邻兵 `(6,1)` 仍存活，扩散0/8，验证不是默认范围攻击。
- 失败原因：初始炮点将 `(3,7)` 提示横竖同线限制；点 `(6,1)` 提示多个炮架；移友军马 `(3,1)` 至 `(4,3)` 后点车 `(5,1)` 提示没有炮架。各非法选择均保留炮 AP1、禁用执行。
- 敌军炮架：上述移马后，改点 `(6,1)`，预览明确显示敌军车 `(5,1)` 作炮架，执行只击杀目标兵，车仍留在场上。
- 移动消耗：独立重置后炮 `(2,1)` 移至 `(1,1)`，AP0、动作提示已用、无可执行落点、执行禁用。保持移动和攻击二选一，不增加行动次数。
- 复跑命令：`node tests/rules.test.cjs` 18/18；`node tests/replay.test.cjs` 2/2（马、炮两波正式回放）；`node tests/ui-headless.test.cjs` 2/2；`node tests/static-check.cjs` 38 DOM / 19 API；`git diff --check` 通过（仅已有 CRLF 提示）。真实浏览器控制台 error/warn 为空。本轮 P1–P8 相关规则为自动回归，未逐个重新做浏览器试玩。
- 旧局保护：未选择、刷新、关闭、导航或操作用户已有游戏标签，未注入状态、未清存储、未改全局视口，未停止已有服务。在确认 8766 无监听后仅启动绑定 `127.0.0.1` 的独立静态服务。旧标签仍运行原已加载代码，不宣称已热更新。
- 用户入口：`http://127.0.0.1:8766/?fixture=P3&variant=basic-growth`。仅本次新标签恢复零升级初始状态，已选好 `(2,4)` 第一发预览并保留页面；用户可点“执行预览”。若另开入口则先选炮、再选该兵。启动命令：`python -m http.server 8766 --bind 127.0.0.1 --directory web`（已有监听时不要重复启动）。

## Agent Handoff Audit

| Audit ID | Time (UTC+8) | Event | From role / thread ID | To role / thread ID | Message ID or title | Summary | Evidence links | Result |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| H-US-001-C-001 | 2026-09-19T00:38:02+08:00 | FIX_HANDOFF | 策划 / 01a06c5f-88f7-7b40-bc29-6e2885783326 | 开发 / 01a07037-771d-7322-9ffc-00310bb38c60 | 开发修复交接｜US-001 炮基础攻击｜保留用户当前棋局 | 用户确认单目标并授权修复；交付基础攻击说明、AP门控、具体失败原因与零SP独立体验入口；严禁操作现有棋局，开发后直接交策划 | 本文；af25dfa；纯规则反例 | 消息成功；待开发接手、实现和独立页面验收 |
| H-US-001-C-002 | 2026-09-19T00:39:00+08:00 | ACK | 开发 / 01a07037-771d-7322-9ffc-00310bb38c60 | 策划 / 01a06c5f-88f7-7b40-bc29-6e2885783326 | ACK｜炮基础攻击修复 | 已核对补充合同、基线和保护旧局硬约束；接手实现，承诺只做代码与纯规则 / UI 桩验证 | 本文；开发会话消息 | ACK；开发继续执行 |
| H-US-001-C-003 | 2026-09-19T00:48:36+08:00 | DEV_COMPLETE_HANDOFF | 开发 / 01a07037-771d-7322-9ffc-00310bb38c60 | 策划 / 01a06c5f-88f7-7b40-bc29-6e2885783326 | 炮基础攻击修复完成，交策划独立标签验收 | 基于 `af25dfa` 完成 AP 门控、基础攻击说明、炮架 / 连线 / 主目标预览、具体失败原因和零 SP 成长夹具；开发未操作任何浏览器标签或服务 | `7392116`；本文；`docs/web-m0-delivery.md`；规则 18/18、回放 2/2、静态 / UI 桩通过 | 开发完成；冻结代码，待策划在 127.0.0.1:8766 独立标签验收 |
| H-US-001-C-004 | 2026-09-20T21:49:53+08:00 | HANDOFF_ACK | 策划 / 01a06c5f-88f7-7b40-bc29-6e2885783326 | 开发 / 01a07037-771d-7322-9ffc-00310bb38c60 | ACK：恢复炮补充修复验收 | 前次额度中断未形成结论；已核对冻结版 7e82212，web/tests 无差异；仅新建独立标签验收，不操作用户现有游戏标签 | 本文；7e82212；开发会话 ACK 消息 | 已确认接手，正在验收 |
| H-US-001-C-005 | 2026-09-20T21:53:26+08:00 | PRODUCT_ACCEPTED | 策划 / 01a06c5f-88f7-7b40-bc29-6e2885783326 | 开发 / 01a07037-771d-7322-9ffc-00310bb38c60 | PRODUCT_ACCEPTED：仅炮补充修复 | 独立浏览器完成零SP基础射击、双方炮架、拒绝边界、行动耗尽、真实成长购买与次回合扩散；旧局未操作。仅本修复完成，当前炮可玩；父US/M0整体未最终验收，下一项父US剩余策划验收待继续，不新增开发范围 | 本文策划验收记录；7e82212；自动回归18/18、2/2、2/2、静态通过 | 本修复Completed；验收结论已成功通知开发 |
