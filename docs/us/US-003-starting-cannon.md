# US-003：初始队伍增加炮

状态：Ready for development。基线：`46e0052`（US-002 已开发交付，尚未策划验收）。
工作目录：`D:\Projects\mihuayou\gamehack2026`；分支：`codex/web-gameplay-validation`。
流程：策划 → 开发 → 策划验收；开发角色实现，策划不写功能代码。

## 用户故事与范围

用户要求“初始我方加个炮，我来验证炮的功能，让开发去写代码”。因此新版默认正式新局（不是仅测试夹具）初始队伍为 **帅 + 车 + 炮**，可直接验证基础攻击和击杀成长。沿用 [US-002 9×9棋盘](US-002-board-9x9.md) 与 [炮基础攻击](US-001-cannon-fix.md)；仅初始队伍及必要的双炮兼容覆盖旧规格，其他规则不变。

- 初始炮出生 `(1,2)`，独立稳定 ID（例如 `starting-cannon`）；Lv1、SP0、HP2/2、攻击2、AP1、扩散0/8、击杀0，免费加入，不扣金币或占首波后的购买名额。
- 帅 `(4,0)`、车 `(0,0)` 不动，敌方配置不变。按当前第一波布阵，可选初始炮 `(1,2)` → 敌炮 `(1,7)`，中间恰好敌兵 `(1,5)` 作架，零升级即可造成2伤害并获得1SP；炮架不受伤。这是正常正式局规则，不注入资源或改敌人属性。
- 保持移动最多3格、移动和攻击共用1AP、基础单格伤害2、炮架可敌可友、既有扩散与友伤规则；不额外赠送技能点或升级。
- 首波后商店仍可买马或炮，价格2金币、最多买1枚、允许跳过。商店炮仍部署 `(2,2)`，初始炮仍部署 `(1,2)`，ID不得重复，选择/成长/升级/预览必须分别绑定正确棋子。
- 初始炮存活跨波时继承等级、SP、技能和剩余HP，重置行动额度并回到初始炮自己的出生格；阵亡不复活。若购买商店炮，不覆盖初始炮；若初始炮已阵亡，可正常买新炮，新炮从Lv1/SP0开始。
- 重新开始恢复三枚初始我军且只含一枚初始炮，所有成长清零；不得把上一局购买炮留下。
- 保留 P1–P8、河道外缘入口和炮 basic-growth 测试场景原本用途；不能给每个夹具无条件塞入初始炮影响测试。

## 非目标与旧局保护

不改9×9尺寸/河道/九宫、不改技能/价格/敌阵/两波流程、不新增商店机制、不做存档/热更新/部署。不操作任何用户已有游戏或测试标签，不刷新/导航/关闭/注入，不清存储、不改全局视口、不重启已有服务。新版在独立新标签生效，旧页保留已加载状态。

## 验收条件

| 编号 | 期望与验证 |
| --- | --- |
| AC01 | 默认新局恰有帅车炮3枚我军、8枚敌军，初始炮属性与坐标符合上述规则，UI可正常选中；0金币0SP。 |
| AC02 | 新局初始炮能直接预览并提交攻击(1,7)，显示敌兵(1,5)炮架、单目标预期伤害2；执行后炮Lv2/SP1/AP0、敌炮死亡而敌兵存活。可花真实所得SP购买扩散，次回合恢复AP；不注入资源。 |
| AC03 | 购买马/购买第二炮/跳过/初始炮阵亡后购炮均正确，金币与购买次数不变；双炮ID唯一、位置不重叠、独立选择/升级/成长、跨波不互相覆盖。 |
| AC04 | 跨波继承和死亡不复活通过；重开恢复干净三枚初始我军；无状态残留。 |
| AC05 | 更新正式马/炮购买分支的无资源注入两波回放（新版初始队伍断言改为3枚），规则、UI桩、静态及语法检查全绿；9×9和炮修复现有回归不退化。 |
| AC06 | 开发完成后提供新版默认入口及直接验证步骤，冻结代码交策划独立标签验收；保护所有旧标签和已有服务。 |

## 与 US-002 的交付关系

US-002 仍为 Ready for product acceptance，尚未验收通过。本次新增需求授权在其 `46e0052` 交付上继续开发，解除该工作区为旧验收保持冻结的要求；不把新请求误记成US-002缺陷。US-003交付后，由策划在新冻结版分别核对US-002空间规则与US-003新增初始炮，分别给出结论，父M0仍保持实际状态。

## 交付约定

仅修改必要 `web/`、相关测试/回放、本US与交付说明，不重构通用多单位系统。保留 AGENTS.md、角色登记、US-001未提交修改及 m0-zyf/、outputs/ 和其他并行任务文件；禁止整目录暂存、切分支、推送、合并或部署。

完成后直接交策划 `01a06c5f-88f7-7b40-bc29-6e2885783326`：冻结提交、变更清单、测试结果、默认入口、试玩步骤、遗留风险。不向历史测试任务交接。开发只代码/自动测试，真实浏览器由策划使用新标签验收。

## 实现与验证记录

待开发接手。基线9×9开发自测为规则19/19、回放2/2、UI桩2/2、静态通过；不是本US的新实现证据，也不代表9×9策划验收通过。

## Agent Handoff Audit

| Audit ID | Time (UTC+8) | Event | From role / thread ID | To role / thread ID | Message ID or title | Summary | Evidence links | Result |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
