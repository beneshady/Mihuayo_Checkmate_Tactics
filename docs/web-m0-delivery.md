# US-001 网页原型交付说明

版本：web-us003-v1.3｜日期：2026-09-21｜分支：`codex/web-gameplay-validation`

本版按 [US-002](us/US-002-board-9x9.md) 将原型迁移为 9×9 棋盘、中央可通行河道、居中双方九宫，保留士、我方成长与技能、兵转职、车双段动作、炮八邻扩散、两波正式流程及 P1–P8 夹具。技术仅用 HTML、JavaScript 和仓库内 three.js r128 UMD 文件，无构建步骤、外部素材或网络依赖。

本版再按 [US-003](us/US-003-starting-cannon.md) 将免费初始炮加入正式新局。默认我方为帅 `(4,0)`、车 `(0,0)`、初始炮 `(1,2)`；商店炮仍为独立棋子，出生 `(2,2)`。

## 启动

- 直接打开 `web/index.html`。
- 或在仓库根目录运行 `python -m http.server 8765 --directory web`，访问 `http://127.0.0.1:8765/`。
- 夹具入口：`?fixture=P1` 至 `?fixture=P8`；变体使用 `&variant=river-edge`、`blocked`、`spearman`、`kill`、`survive`、`enemy-empty`、`mate` 或 `basic-growth`。其中 `?fixture=P1&variant=river-edge` 用于河道与外缘点选，`?fixture=P6&variant=mate` 是正向将死入口，`?fixture=P3&variant=basic-growth` 是炮零 SP 成长入口。正式局 URL 不带 fixture 参数，不注入兵、SP 或升级。

## 文件

| 文件 | 内容 |
| --- | --- |
| `web/js/rules.js` | 无 DOM 的确定性规则、意图重验证、预览、将死搜索、商店和夹具 |
| `web/js/app.js` | three.js 2.5D 棋盘、点选、高亮、技能与方向面板、暂停和覆盖层 |
| `web/index.html` | 页面结构与桌面 / 窄屏响应式样式 |
| `tests/rules.test.cjs` | T01–T60 映射组及士专项规则测试 |
| `tests/find-playthrough.cjs` | 正式局 beam search 回放生成器 |
| `tests/replay.test.cjs` | 马 / 炮两条正式局回放复验 |
| `tests/ui-headless.test.cjs` | 页面脚本加载、夹具、HUD、技能文案与连续重开桩测试 |
| `tests/static-check.cjs` | DOM ID 与规则 API 静态一致性检查 |

### US-003：初始我方增加炮（2026-09-21）

- 默认正式新局现在恰有帅、车、`starting-cannon` 三枚我军和八枚敌军。初始炮为 Lv.1、SP0、HP2/2、攻击2、AP1、扩散0/8，免费且不占第一波后的商店购买名额。
- 正式局可直接选择初始炮并攻击 `(1,7)`：敌兵 `(1,5)` 是唯一炮架，敌炮承受2伤害并阵亡，炮架保留；初始炮获得 Lv.2 / SP1，随后可购买扩散，下一回合恢复 AP。
- 商店的 `cannon` 与 `starting-cannon` ID、出生格、成长和技能独立。存活初始炮跨波继承属性并复位至 `(1,2)`；阵亡不复活，重开只恢复三枚初始我军。P1–P8 夹具保持原有棋子配置。
- 自动验证：规则 20/20、重新生成的马/炮正式回放 2/2、UI 桩 2/2、静态检查、两项语法检查和 `git diff --check` 通过。真实浏览器由策划使用新标签验收。

## 自动验证

在仓库根目录逐条执行：

```text
node tests/rules.test.cjs
node tests/find-playthrough.cjs
node tests/replay.test.cjs
node tests/static-check.cjs
node tests/ui-headless.test.cjs
```

2026-09-16 本机 Node v24.15.0 结果：

| 检查 | 结果 |
| --- | --- |
| T01–T60 映射组 + 士专项 | 16/16 通过 |
| 正式回放 | 马、炮 2/2 通过 |
| 马分支 | 22 步，8 回合，6 杀，最高棋子 Lv.5；首波购买冲击延伸；马第二波造成伤害 |
| 炮分支 | 13 步，5 回合，3 杀，最高棋子 Lv.3；首波购买冲击延伸；炮第二波造成伤害并斩将 |
| 静态检查 | 38 个 DOM ID、18 个规则 API 对齐 |
| UI 桩测试 | 2/2 通过 |
| `git diff --check` | 通过；仅有仓库既有 LF/CRLF 提示 |

### US-002：9×9 棋盘与中央河道（2026-09-20）

- 全部空间配置、绘制、点选换算、AI、搜索、波次重部署、夹具及溅射裁边统一为 9×9。坐标为 `0..8`，`y=4` 是可正常落子与攻击的河道。
- 我方兵系在 `y>=5` 才过河，敌方在 `y<=3` 才过河；河道按动作起点仍未过河。双方九宫迁至 `x=3..5`，我方 `y=0..2`、敌方 `y=6..8`。
- 正式部署迁至合同坐标；马/炮两条零资源两波回放已重新生成并通过。炮仍可隔恰好一子跨 8 步攻击，车/弓/枪的 7 格数值上限未改变。
- 自动验证：规则 19/19、正式回放 2/2、UI 桩 2/2、静态检查 38 DOM / 19 API、语法和 `git diff --check` 通过。真实浏览器尚待策划使用独立标签检查第九行/列、四角、河道和预览点选。

### PA-9-01：9×9 格盘点选尺度修复（2026-09-21）

- 棋盘纹理的外圈是装饰，格盘实际只占板面内缩区域。棋子、合法落点标记和射线点选已统一使用该实际格盘的单格宽度，外圈不会再落入棋位。
- 自动几何回归遍历81格中心、四边内缩、四角与第9行/列，并确认四侧装饰边框不能落子。规则20/20、两条正式回放2/2、UI桩3/3、静态检查与语法检查通过。
- 真实复验入口：`http://127.0.0.1:8766/?fixture=P1&variant=river-edge`。选择车 `(0,0)` 的普通移动后，分别点可见 `(0,2)` 格中心和右侧内缩位置；两者都应预览 `(0,2)`。再检查四角、第9行/列与河道。

### US-004：直接移动、撤销与敌方意图悬停（2026-09-22）

- 合法的普通移动和车疾行现在点格即执行。攻击、方向攻击和车冲击（包含冲向空格）仍先预览，再用“执行攻击”确认。
- “撤销移动”按全队本回合未锁定纯移动的倒序恢复位置和行动额度。攻击、成功升级和确认结束回合锁定既有移动；重开、跨波、终局和进入商店也会清理记录。若纯移动让帅受威胁，会保留撤销入口。
- 悬停敌方棋子显示现有意图的来源、路径、目标、方向及重验证状态；不重算 AI。所有选择、移动、攻击、路径、炮扩散与敌意图标记改为对齐实际格盘的整格色块。
- 自动验证：规则20/20、正式回放2/2、UI桩5/5（含撤销、危险移动、悬停和整格覆盖）、静态40 DOM / 20 API、语法和 `git diff --check` 通过。真实浏览器仍待策划新标签试玩。

### 炮基础攻击补充修复（2026-09-19）

- 零 SP、零扩散的炮仍可横竖隔恰好一个炮架，对单个主目标造成 2 伤害；友军或敌军都可作为炮架。
- 炮选择卡明确显示“基础攻击：单格 / 伤害 2 / 隔一子攻击”、本回合行动状态，以及“扩散 0/8 不影响基础攻击”。合法攻击预览会列出炮架、攻击连线、主目标和预期伤害。
- 非车单位 AP 耗尽后，`legalActions` 不再返回伪可执行动作。炮会提示“本回合已行动，下回合可攻击”；无炮架、多个炮架和不在同一直线分别提示原因。
- 独立入口：`http://127.0.0.1:8766/?fixture=P3&variant=basic-growth`。炮初始 Lv.1、SP 0、扩散 0/8、AP 1；先攻击 `(2,4)`，以 `(2,2)` 友军马为炮架。击杀后得到 1 SP，购买 `扩散 E`，结束回合；再攻击 `(5,1)`，以 `(3,1)` 友军马为炮架，同时命中 `(6,1)` 扩散格。敌将保留，过程不会首次击杀即结束。
- 本轮自动检查：规则 18/18、正式马/炮回放 2/2、静态检查 38 个 DOM ID / 19 个规则 API、UI 桩 2/2、语法和 `git diff --check` 均通过。开发未操作用户现有浏览器标签；真实浏览器结果由策划在独立标签追加。

本轮针对策划验收 PA-01～PA-05 增加了直接回归：桌面棋盘区与左右控件区分离；窄屏意图展开入口包含顺序、起点、目标和预测状态；车预览覆盖空冲、击杀占位、存活停在接近点和震退受阻；炮预览列出阵营、受害格、HP 前后、阵亡及友军误伤警告；敌车锁定冲击的目标变空后仍移动到原目标格且状态为 `success`。`P8&variant=enemy-empty` 可在页面复现最后一项。

`node --test tests` 在本沙箱因 Node test runner 创建子进程时报 `spawn EPERM`。各测试文件直接运行均通过；这是运行环境限制，不是用例失败。

## 真实浏览器验证

`207fab0` 的历史浏览器记录已被策划验收 PA-01～PA-05 推翻，不能作为本轮通过证据。本轮开发会话的浏览器入口因账户额度自动审查不可用，因此这里只记录可重复的投影检查：1280×800 的独立棋盘区为 814×800，1280×720 为 814×720，390×844 的棋盘区约为 390×439；棋盘四角投影在三种尺寸下均处于相机 NDC 范围内，且桌面棋盘区不与左侧 286px 面板、右侧 150px 操作栏重叠。

冻结提交交测试后，必须在真实浏览器补齐并追加证据：1280×800、1280×720、390×844；手机意图展开；P2 / P3 / P4 / P5 / P8 及变体实际执行；主帅阵亡与将死；马 / 炮两条完整两波胜利；暂停和连续重开。未完成该轮实测前，不标记本 US 验收通过。

## 操作说明

1. 点棋盘棋子或左侧单位。车先选“普通移动”或“冲击”；弓 / 枪点前、右、后、左。
2. 格子圆点为当前模式合法目标。选择后先看路径、命中、震退和敌方阶段预测，再执行。
3. 技能按钮显示价格、当前值和禁用原因。方向面板明确区分棋子等级与每方向等级 / 攻击距离。
4. 第一波斩将后可买马或炮；也可返回棋盘为存活单位升级，再进入第二波。
5. 暂停、切后台自动暂停；重开恢复当前正式局或当前夹具的初始状态。

## 已知限制与 TBD

- 敌方出生坐标未调整，保持 US-001 第 2.4 节种子；正式路径已证明可通关，但平衡仍是 TBD。
- 将死搜索提供可中断生成器；UI 仅在当前确有主帅威胁时同步完成分析。未完成的生成器不改状态，也不判负。
- 敌方阶段当前即时刷新棋位和结果，没有逐棋子的播片动画；规则效果与输入锁已分离，动画节奏留待后续体验迭代。
- P 夹具是开发 / 测试入口，不属于正式关卡菜单。
- `m0-zyf/`、`AGENTS.md` 与角色登记的并行改动未纳入本功能修改。
