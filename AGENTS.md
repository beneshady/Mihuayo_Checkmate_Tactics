# 项目规则

## 开发目标

- 本分支仅开发浏览器可运行的游戏原型，用于前期验证玩法、规则及后续策划案。
- 优先让游戏逻辑和最小可玩流程跑通；允许简单界面与占位素材，不追求正式美术或完整框架。
- 只使用网页运行所需的技术，不引入游戏引擎、平台 SDK、专用编辑器或相关工具链。
- 未验证的设计标记为 `TBD`；规则有歧义时先说明，不把假设当成既定策划。

## 开发代码的四个原则

### 1. 编码前思考

- 开始前确认理解任务；非平凡任务说明关键假设、歧义和明显更简单的方案。
- 不默默选择高影响的解释并直接实现；局部、低风险、易撤销的歧义采用最简单的合理假设，并在结果中说明。

### 2. 简洁优先

- 只写解决当前问题所需的最少代码。`Simple > Clever`。
- 不添加未要求的功能、抽象或灵活性，不为假设中的未来需求建立系统。
- 实现明显比问题复杂时，先简化。

### 3. 精准修改

- 只修改任务所需内容，保持现有风格，保留用户无关改动。
- 不顺便重构、格式化或删除无关代码；只清理本次修改造成的无用代码。
- 每一处差异都应能由当前任务解释。

### 4. 目标驱动执行

- 实现前定义可观察、可验证的验收标准。
- 非平凡任务遵循“计划 → 实现 → 验证”；修复问题遵循“复现 → 修复 → 验证”；重构前后都要验证。
- 持续验证直到满足验收标准；未通过或无法执行的检查应如实报告。

## 最小交付约定

- 项目文档使用中文，API、代码及标准技术标识可保留英文。
- 游戏规则与页面展示尽量分离，便于独立测试；不为此预建复杂框架。
- 玩法变更记录对应规则测试与浏览器试玩方法；交付说明启动方式、验证结果和待确认事项。
- 在功能分支开发，不直接修改 `main`；未经明确要求，不发布、不部署，也不恢复旧项目工具链。

## 会话协作工作流

- 默认流程：**策划 → 开发 → 策划验收**。项目管理负责协调、阻塞和交接追踪；`cocos技术顾问` 不参与本分支的默认流程，不设置独立测试阶段或测试会话交接。
- 策划将讨论整理为明确的需求、范围、不做事项和可验证的验收条件；用户明确进入实施后，自动唤醒开发。纯讨论或仍有关键歧义时不派发开发。
- 开发每次完成一个明确任务，实现、完成必要验证并记录修改与证据后，直接自动唤醒策划验收。
- 策划依据交付版本逐项核对验收条件，通过浏览器试玩验收玩法与体验；不通过则说明差距并直接交给开发修复，修复后再次交给策划验收。只标记本次已验收任务完成，不据此宣称整个父需求完成。
- 已进入实施的任务，其开发、修复和策划验收交接已获授权，无须用户逐次转述；新增需求或扩大范围仍须回到策划确认。此授权不包含推送、合并、发布或部署。

### 会话定位与交接

已于 2026-09-15 核对本项目会话；项目 ID 为 `8ba29e24-adbb-481f-a07f-ac53c2e038e8`，主机为 `local`。

| 会话名称 | 会话 ID |
| --- | --- |
| 项目管理 | `01a06bbb-6fa7-7cd0-a5a8-34d0f3c4c853` |
| 策划 | `01a06c5f-88f7-7b40-bc29-6e2885783326` |
| 开发 | `01a07037-771d-7322-9ffc-00310bb38c60` |
| cocos技术顾问 | `01a06c41-b6f4-7831-8d5d-e00be7275302` |

- 使用会话列表和读取工具确认目标属于本项目及其当前状态，再调用 `send_message_to_thread` 发送交接并唤醒；ID 失效时按项目和角色重新查找，不向其他项目同名会话发送。
- 交接必须包含：任务标识、需求文档或 Issue、验收条件、工作目录、分支及提交版本（未提交改动需明确列出）、验证结果、遗留问题和要求下一角色执行的动作。接收方先核对并确认接手。
- 需求与证据沿用项目文档及已有 Issue；每次成功交接和接手，在对应任务记录中追加时间、发送方、接收方、版本和结果，避免仅存在聊天里。无 Issue 的任务在对应需求文档记录，不另建一套看板。
- 当前会话共享工作目录；同一交付任务串行交接，策划验收期间冻结交付版本。需要并行开发时使用独立 worktree，并在交接中明确路径，禁止擅自切换其他会话使用的工作区分支。
- 消息发送成功仅表示已派发；通过读取目标会话或 `wait_threads` 确认执行结果。工具不可用或目标不可达时记录阻塞并告知项目管理，不声称已经唤醒或完成。
- 本文是 Agent 执行约定，不是后台调度器；由正在执行的会话主动发送下一阶段消息。本轮只建立规则，不自动派发未指定的功能任务。
